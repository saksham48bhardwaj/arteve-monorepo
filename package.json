{
  "name": "arteve-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/minimatch": "^6.0.0",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "rimraf": "^6.0.1",
    "tailwindcss": "^4.1.17",
    "turbo": "^2.0.6",
    "typescript": "^5.6.2"
  },
  "packageManager": "npm@10.2.3",
  "dependencies": {
    "@supabase/ssr": "^0.7.0",
    "minimatch": "^10.1.1"
  }
}
